// @flow

// This file is code-generated by running:
//
//   $ ignite generators make-screen-map

import { NavigationActions } from 'react-navigation'

/**
 * Creates an navigation action for dispatching to Redux.
 *
 * @param {string} routeName The name of the route to go to.
 */
const navigateTo = routeName => () => NavigationActions.navigate({ routeName })

<% for(var i=0; i<props.screens.length; i++) { 
  const { component, name } = props.screens[i]
-%>
export const navigateTo<%= pascalCase(name) %> = navigateTo('<%= name %>')
<% } -%>
